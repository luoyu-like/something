cmake_minimum_required(VERSION 3.12)
project(tesk4)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
if(MSCV)
    add_compile_options(/w4)
else()
    add_compile_options(-wall Wextra -Wpedantic)
endif()
add_library(autodiff Dual.h)
sdd_executable(test_dual test_dual.cpp)
target_link_libraries(test_dual autodiff)
install(TARGETS autodiff DESTINATION lib)
install(FILES Dual.h DESTINATION include)